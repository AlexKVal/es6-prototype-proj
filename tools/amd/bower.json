{
  "name": "<%= pkg.name %>",
  "version": "<%= pkg.version %>",
  "homepage": "<%= pkg.homepage %>",
  "author": "<%= pkg.author %>",
  "license": "<%= pkg.license %>",
  "main": [
    "es6-prototype-proj.js"
  ],
  "keywords": [
    <%= _.map(pkg.keywords, function(keyword) { return '"' + keyword + '"' }).join(',\n    ')%>
  ],
  "ignore": [
    "**/.*"
  ]
}
